{
  "name": "evil-install",
  "version": "1.0.0",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "postinstall": "node -e \"const p=process.env.SBX_PWN_MARKER; if(p){require('fs').writeFileSync(p,'pwned')}\""
  }
}
